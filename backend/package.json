{
  "name": "docker-prisma-express",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prisma": "^6.7.0"
  }
}
